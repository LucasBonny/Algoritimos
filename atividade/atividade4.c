#include <math.h>
#include <stdio.h>

int main(){
    double capital,taxa ,tempo, resultado;
    printf("Insira o capital: "); 
    scanf("%lf", &capital);
    printf("Insira o tempo em anos: "); 
    scanf("%lf", &tempo);
    printf("Insira a taxa de juros em porcentagem: "); 
    scanf("%lf", &taxa);
    resultado = capital * (pow());
    printf("Seu resultado e: %lf", resultado);
}